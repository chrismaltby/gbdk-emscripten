import Path from "path";

export const GBDK_PATH = Path.join(__dirname, "gbdk");
export const GBDK_INCLUDE_PATH = Path.join(GBDK_PATH, "include");
export const GBDK_LIB_PATH = Path.join(GBDK_PATH, "lib");
